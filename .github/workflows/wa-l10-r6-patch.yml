name: WA-L10 R6 conversation UX patch

on:
  push:
    branches:
      - wa-l10-r6-conversation-ux-fastlane-20260905
    paths:
      - .github/workflows/wa-l10-r6-patch.yml

permissions:
  contents: write

jobs:
  patch-test-commit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: wa-l10-r6-conversation-ux-fastlane-20260905
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Apply scoped R6 product patch
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def replace_once(text, old, new, label):
              if new in text:
                  return text
              if old not in text:
                  raise SystemExit('PATCH_ANCHOR_MISSING:'+label)
              return text.replace(old,new,1)

          # -------- WA4 Copilot: approved Sofia intro + Unicode sanitation + deterministic fast lanes --------
          p=Path('app/wa4-copilot.js')
          s=p.read_text()
          s=replace_once(s,
            "const APPROVED_FIRST_CONTACT_COPY = 'Hablas con el asistente de WhatsApp de Zi Vital. Brindamos embellecimiento facial y corporal, tratamientos que ayudan a sentirte mejor y verte mejor. ¿En qué podemos ayudarte hoy?';\nconst APPROVED_FIRST_CONTACT_PREFIX = 'Hablas con el asistente de WhatsApp de Zi Vital. Brindamos embellecimiento facial y corporal, tratamientos que ayudan a sentirte mejor y verte mejor.';",
            "const APPROVED_FIRST_CONTACT_COPY = '¡Hola! 👋 Soy Sofía de Zi Vital. Claro, te ayudo 😊';\nconst APPROVED_FIRST_CONTACT_PREFIX = APPROVED_FIRST_CONTACT_COPY;",
            'approved-sofia-copy')

          s=s.replace(
            'La capa de salida agrega de forma determinística la presentación inicial aprobada cuando corresponde; no la dupliques. Escribe español natural de WhatsApp: breve, profesional, cálido, pocos emojis funcionales y máximo una pregunta útil al final cuando corresponda.',
            'La presentación inicial aprobada la maneja una capa determinística; no la repitas ni vuelvas a presentar la clínica si ya apareció. Escribe español natural de WhatsApp: breve, profesional, cálido, máximo dos párrafos cortos, pocos emojis funcionales y máximo una pregunta útil al final cuando corresponda. No des una explicación médica larga si el cliente solo pide orientación comercial general.'
          )

          old_render="""function renderWhatsAppText(value){
  return String(value||'').trim()
    .replace(/\\*\\*([^*\\n]+)\\*\\*/g,'*$1*')
    .replace(/__([^_\\n]+)__/g,'_$1_')
    .replace(/^#{1,6}\\s+/gm,'')
    .trim();
}
"""
          new_render="""function canonicalizePatientText(value){
  return String(value||'')
    .replace(/\\uFFFD+/g,' ')
    .replace(/\\bzi\\s+vital\\b/gi,'Zi Vital')
    .replace(/[ \\t]{2,}/g,' ');
}
function renderWhatsAppText(value){
  return canonicalizePatientText(value).trim()
    .replace(/\\*\\*([^*\\n]+)\\*\\*/g,'*$1*')
    .replace(/__([^_\\n]+)__/g,'_$1_')
    .replace(/^#{1,6}\\s+/gm,'')
    .trim();
}
"""
          s=replace_once(s,old_render,new_render,'unicode-renderer')
          s=s.replace("const marker=normalizeText('Hablas con el asistente de WhatsApp de Zi Vital');","const marker=normalizeText('Soy Sofía de Zi Vital');")

          runtime_anchor="function runtimeSummary(r){\n"
          intro_helpers="""function deterministicOwnerApprovedIntroDraft(runtime,inbound,messages){
  if(hasApprovedIntro(messages))return null;
  const intents=new Set(runtime&&Array.isArray(runtime.intents)?runtime.intents:[]);
  if(isGreetingOnly(inbound)){
    return {reply:APPROVED_FIRST_CONTACT_COPY+'\\n\\n¿Ya eres paciente de la clínica o es tu primera vez con nosotros?',intent:'INFO',next_action:'REPLY',confidence:1,cited_knowledge_ids:[],needs_human:false,reason:'Owner-approved organic first-contact copy.'};
  }
  const treatment=String(runtime&&runtime.state&&runtime.state.treatment||'');
  const transactional=['TREATMENT_PRICE','CONSULTATION_PRICE','PRICE_PER_SESSION','PROMOTION_REQUEST','BOOKING','SCHEDULE','RESCHEDULE_INTENT','CONFIRM_BOOKING','PAYMENT'];
  if(treatment==='TOXINA_BOTULINICA'&&!transactional.some(x=>intents.has(x))){
    return {reply:'¡Hola! 👋 Soy Sofía de Zi Vital. Claro, te ayudo con la toxina botulínica ✨\\n\\nPara orientarte mejor, cuéntame qué zona te gustaría mejorar: frente, entrecejo, patitas de gallo o varias zonas.',intent:'INFO',next_action:'REPLY',confidence:1,cited_knowledge_ids:[],needs_human:false,reason:'Owner-approved toxin first-contact copy.'};
  }
  return null;
}

"""+runtime_anchor
          s=replace_once(s,runtime_anchor,intro_helpers,'intro-helper')

          kq_anchor="""function knowledgeQuery(inbound,runtime){
  const s=runtime&&runtime.state||{};
  return [String(inbound||''),s.treatment||'',s.campaign_source||''].filter(Boolean).join(' | ').slice(0,5000);
}
"""
          fast_helpers=kq_anchor+"""function withDeadline(promise,ms,code){
  let timer;
  const timeout=new Promise((_,reject)=>{timer=setTimeout(()=>reject(new Error(code||'WA4_DEADLINE_EXCEEDED')),Math.max(250,Number(ms)||2500));});
  return Promise.race([Promise.resolve(promise),timeout]).finally(()=>clearTimeout(timer));
}
function isPriceFastLane(runtime){
  const intents=new Set(runtime&&Array.isArray(runtime.intents)?runtime.intents:[]);
  const treatment=String(runtime&&runtime.state&&runtime.state.treatment||'');
  if(treatment!=='TOXINA_BOTULINICA'||!intents.has('TREATMENT_PRICE'))return false;
  return !['PROMOTION_REQUEST','BOOKING','SCHEDULE','RESCHEDULE_INTENT','CONFIRM_BOOKING','CONSULTATION_PRICE','PRICE_PER_SESSION'].some(x=>intents.has(x));
}
function deterministicToxinPriceDraft(publicBundle,processContexts){
  const ctx=new Map((Array.isArray(processContexts)?processContexts:[]).filter(x=>x&&x.entity_id).map(x=>[String(x.entity_id),x]));
  const options=[];
  for(const item of (publicBundle&&Array.isArray(publicBundle.items)?publicBundle.items:[])){
    if(!item||item.domain!=='CATALOG')continue;
    const id=playbooks.catalogId(item),p=id?ctx.get(id):null;
    if(!id||!p||p.ready_for_quote!==true||String(p.price_state||'')!=='READY'||String(p.freshness_state||'')==='STALE_REVIEW')continue;
    if(!normalizeText(p.category||(item.facts&&item.facts.categoria)).includes('toxina'))continue;
    const name=String(p.entity_name||item.title||'').trim();
    const nm=normalizeText(name),brand=/hutox/.test(nm)?'HUTOX':(/nabota/.test(nm)?'NABOTA':null);
    const zm=nm.match(/\\b([13])\\s+zonas?\\b/),um=name.toUpperCase().match(/\\b(\\d+)U\\b/);
    if(!brand||!zm)continue;
    const currency=String(p.moneda||'').toUpperCase();
    const price=p.quote_price!=null?p.quote_price:(p.precio_oferta!=null?p.precio_oferta:p.precio_base);
    const priceLabel=moneyLabel(currency,price);
    if(!priceLabel)continue;
    const zones=Number(zm[1]);
    options.push({knowledge_id:String(item.knowledge_id),brand,zones,units:um?um[1]+'U':null,price:Number(price),priceLabel});
  }
  options.sort((a,b)=>a.zones-b.zones||a.price-b.price||a.brand.localeCompare(b.brand));
  const unique=[];const seen=new Set();
  for(const o of options){const k=o.brand+':'+o.zones;if(seen.has(k))continue;seen.add(k);unique.push(o);if(unique.length>=4)break;}
  if(unique.length<2)return null;
  const lines=unique.map(o=>'• '+o.brand+' · '+o.zones+(o.zones===1?' zona':' zonas')+(o.units?' ('+o.units+')':'')+': '+o.priceLabel);
  return {reply:'Claro 😊 Para toxina botulínica, estos son los precios vigentes que tengo confirmados:\\n'+lines.join('\\n')+'\\n\\n¿Qué zona te gustaría tratar?',intent:'PRICE',next_action:'REPLY',confidence:1,cited_knowledge_ids:unique.map(o=>o.knowledge_id),needs_human:false,reason:'Deterministic READY/FRESH toxin price fast lane.'};
}
async function buildFastPriceContext(serviceRpc,serviceGet,inbound,runtime){
  const query=knowledgeQuery(inbound,runtime);
  const rows=await withDeadline(searchKnowledge(serviceRpc,query,'PUBLIC_CLIENT',8,['CATALOG','CATEGORY']),3500,'WA4_FAST_PRICE_SEARCH_TIMEOUT');
  const raw=knowledge.buildKnowledgeBundle(rows,8,'PUBLIC_CLIENT');
  const contexts=await withDeadline(loadProcessContexts(serviceGet,catalogIdsFromBundles(raw)),1800,'WA4_FAST_PRICE_CONTEXT_TIMEOUT');
  return {publicBundle:gatePublicCatalogMoney(raw,contexts,'PRICE_QUOTE',runtime),processContexts:contexts};
}
"""
          s=replace_once(s,kq_anchor,fast_helpers,'fast-price-helpers')

          insert_anchor="""      const clinicalRisk=ai.personalizedClinicalRisk(inbound);
      const [campaignCtx,identityCtx]=await Promise.all([
"""
          insert_block="""      const clinicalRisk=ai.personalizedClinicalRisk(inbound);

      const introDraft=!clinicalRisk?deterministicOwnerApprovedIntroDraft(runtime,inbound,messages):null;
      if(introDraft){
        const patientReply=renderWhatsAppText(introDraft.reply);
        Promise.resolve(log({conversation_id:id,actor_id:auth.actor_id,task:'SALES_PLAYBOOK',provider:'deterministic',model:'DETERMINISTIC_OWNER_COPY',safety_model:null,outcome:'SUGGESTED',input_messages:messages.length,input_chars:inbound.length,output_chars:patientReply.length,prompt_tokens:0,completion_tokens:0,total_tokens:0,estimated_cost_usd:0,latency_ms:Date.now()-started,safety_action:'REPLY',safety_category:'OWNER_APPROVED_COPY'})).catch(()=>{});
        return writeJson(res,200,{ok:true,runtime:runtimeSummary(runtime),contexts:{campaign:null,identity:null,booking:null},suggestion:Object.assign({},introDraft,{reply:patientReply}),needs_human:false,next_action:'REPLY',model:'DETERMINISTIC_OWNER_COPY',estimated_cost_usd:0,latency_ms:Date.now()-started,auto_send:false});
      }

      if(!clinicalRisk&&isPriceFastLane(runtime)){
        try{
          const fast=await buildFastPriceContext(serviceRpc,serviceGet,inbound,runtime);
          const draft=deterministicToxinPriceDraft(fast.publicBundle,fast.processContexts);
          if(!draft)throw new Error('WA4_FAST_PRICE_EVIDENCE_REQUIRED');
          const grounded=knowledge.validateGroundedSuggestion(draft,fast.publicBundle);
          if(!grounded.ok)throw new Error(grounded.error||'WA4_FAST_PRICE_GROUNDING_FAILED');
          const patientReply=composePatientReply(grounded.reply,messages,inbound);
          const contexts={campaign:null,identity:null,booking:null};
          const quality=qualityCheck(patientReply,runtime,contexts,inbound,fast.publicBundle);
          if(!quality.ok)throw new Error('WA4_FAST_PRICE_QUALITY_'+quality.violations.join('|').slice(0,80));
          Promise.resolve(log({conversation_id:id,actor_id:auth.actor_id,task:'SALES_PLAYBOOK',provider:'deterministic',model:'DETERMINISTIC_PRICE_FASTLANE',safety_model:null,outcome:'SUGGESTED',input_messages:messages.length,input_chars:inbound.length,output_chars:patientReply.length,prompt_tokens:0,completion_tokens:0,total_tokens:0,estimated_cost_usd:0,latency_ms:Date.now()-started,safety_action:'REPLY',safety_category:'FAST_PRICE_READY'})).catch(()=>{});
          return writeJson(res,200,{ok:true,runtime:runtimeSummary(runtime),contexts,quality,suggestion:Object.assign({},draft,{reply:patientReply,cited_knowledge_ids:grounded.citations}),needs_human:false,next_action:'REPLY',model:'DETERMINISTIC_PRICE_FASTLANE',estimated_cost_usd:0,latency_ms:Date.now()-started,auto_send:false});
        }catch(e){
          Promise.resolve(log({conversation_id:id,actor_id:auth.actor_id,task:'SALES_PLAYBOOK',provider:'deterministic',model:'DETERMINISTIC_PRICE_FASTLANE',safety_model:null,outcome:'BLOCKED',input_messages:messages.length,input_chars:inbound.length,output_chars:0,prompt_tokens:0,completion_tokens:0,total_tokens:0,estimated_cost_usd:0,latency_ms:Date.now()-started,safety_action:'HUMAN_COMMERCIAL',safety_category:'FAST_PRICE_UNAVAILABLE',error_code:String(e&&e.message||'WA4_FAST_PRICE_UNAVAILABLE').slice(0,120)})).catch(()=>{});
          return writeJson(res,503,{ok:false,error:'WA4_FAST_PRICE_UNAVAILABLE',needs_human:true,next_action:'HUMAN_COMMERCIAL',runtime:runtimeSummary(runtime),contexts:{campaign:null,identity:null,booking:null},auto_send:false});
        }
      }

      const [campaignCtx,identityCtx]=await Promise.all([
"""
          s=replace_once(s,insert_anchor,insert_block,'fast-lanes-before-adapters')

          old_exports="module.exports={createCopilot,buildGovernedContext,gatePublicCatalogMoney,adapterSummary,deterministicBookingDraft,deterministicNoPromotionDraft,qualityCheck,renderWhatsAppText,composePatientReply,hasApprovedIntro,isGreetingOnly,APPROVED_FIRST_CONTACT_COPY,APPROVED_FIRST_CONTACT_PREFIX,SALES_SCHEMA,SAFETY_SCHEMA};"
          new_exports="module.exports={createCopilot,buildGovernedContext,buildFastPriceContext,gatePublicCatalogMoney,adapterSummary,deterministicBookingDraft,deterministicNoPromotionDraft,deterministicOwnerApprovedIntroDraft,deterministicToxinPriceDraft,isPriceFastLane,qualityCheck,canonicalizePatientText,renderWhatsAppText,composePatientReply,hasApprovedIntro,isGreetingOnly,APPROVED_FIRST_CONTACT_COPY,APPROVED_FIRST_CONTACT_PREFIX,SALES_SCHEMA,SAFETY_SCHEMA};"
          s=replace_once(s,old_exports,new_exports,'copilot-exports')
          p.write_text(s)

          # -------- L10 bridge: best-effort real Meta typing indicator before model work --------
          p=Path('app/wa-l10-autonomous-bridge.js'); b=p.read_text()
          b=replace_once(b,
            "  const {serviceRpc,suggestInternal,autoSend,requestHandoff}=deps;",
            "  const {serviceRpc,suggestInternal,autoSend,requestHandoff,sendTyping}=deps;",
            'bridge-typing-dep')
          typing_anchor="""    const started=Date.now();

    try{
      const suggestionResult=await suggestInternal(claim.conversation_id);
"""
          typing_block="""    const started=Date.now();

    if(typeof sendTyping==='function'){
      try{
        const typingPromise=sendTyping(claim.provider_message_id);
        if(typingPromise&&typeof typingPromise.catch==='function')typingPromise.catch(e=>log.error&&log.error('[WA-L10-BRIDGE] typing indicator failed',cleanReason(e&&e.message)));
      }catch(e){log.error&&log.error('[WA-L10-BRIDGE] typing indicator failed',cleanReason(e&&e.message));}
    }

    try{
      const suggestionResult=await suggestInternal(claim.conversation_id);
"""
          b=replace_once(b,typing_anchor,typing_block,'bridge-typing-before-suggest')
          p.write_text(b)

          # -------- WA4 server: Meta typing-only transport (not a second message sender) --------
          p=Path('app/server-wa4.js'); w=p.read_text()
          w=replace_once(w,
            "const WA_L4_INTERNAL_TOKEN=process.env.WA_L4_INTERNAL_TOKEN||'';",
            "const WA_L4_INTERNAL_TOKEN=process.env.WA_L4_INTERNAL_TOKEN||'';\nconst WA_ACCESS_TOKEN=process.env.WHATSAPP_ACCESS_TOKEN||'';\nconst WA_PHONE_NUMBER_ID=process.env.WHATSAPP_PHONE_NUMBER_ID||'';\nconst WA_GRAPH_VERSION=process.env.WHATSAPP_GRAPH_VERSION||'';",
            'server-wa-meta-config')
          bridge_anchor="""const bridge=createAutonomousBridge({serviceRpc,suggestInternal,autoSend:body=>internalPost('/api/wa/auto-send',body),requestHandoff,log:console});
"""
          typing_server="""function sendTypingIndicator(providerMessageId){
  return new Promise(resolve=>{
    const messageId=String(providerMessageId||'').trim();
    if(!messageId||!WA_ACCESS_TOKEN||!WA_PHONE_NUMBER_ID||!/^v\\d+\\.\\d+$/.test(WA_GRAPH_VERSION))return resolve({ok:false,skipped:true});
    const payload=JSON.stringify({messaging_product:'whatsapp',status:'read',message_id:messageId,typing_indicator:{type:'text'}});
    const q=https.request({hostname:'graph.facebook.com',path:'/'+WA_GRAPH_VERSION+'/'+encodeURIComponent(WA_PHONE_NUMBER_ID)+'/messages',method:'POST',headers:{Authorization:'Bearer '+WA_ACCESS_TOKEN,'Content-Type':'application/json','Content-Length':Buffer.byteLength(payload),'User-Agent':'AscendaOS-WA-L10-Typing/1.0'},timeout:3000},r=>{r.resume();r.on('end',()=>resolve({ok:r.statusCode>=200&&r.statusCode<300,status:r.statusCode||0}));});
    q.on('timeout',()=>{q.destroy();resolve({ok:false,error:'META_TYPING_TIMEOUT'});});
    q.on('error',()=>resolve({ok:false,error:'META_TYPING_UNAVAILABLE'}));
    q.write(payload);q.end();
  });
}
const bridge=createAutonomousBridge({serviceRpc,suggestInternal,autoSend:body=>internalPost('/api/wa/auto-send',body),requestHandoff,sendTyping:sendTypingIndicator,log:console});
"""
          w=replace_once(w,bridge_anchor,typing_server,'server-typing-wire')
          p.write_text(w)

          # -------- R6 regression suite --------
          r6=Path('ci/wa4-ai-sales-router/r6-conversation-ux-fastlane.test.js')
          r6.write_text("""'use strict';
const test=require('node:test');
const assert=require('node:assert/strict');
const fs=require('node:fs');
const copilot=require('../../app/wa4-copilot');
const knowledge=require('../../app/wa4-knowledge');

const H1='11111111-1111-4111-8111-111111111111';
const N1='22222222-2222-4222-8222-222222222222';
const H3='33333333-3333-4333-8333-333333333333';
const N3='44444444-4444-4444-8444-444444444444';
function item(id,name){return {knowledge_id:'service:'+id,domain:'CATALOG',title:name,facts:{tipo:'SERVICIO',nombre:name,categoria:'TOXINA'},authority_tier:10,freshness_state:'FRESH',conflict_state:'CLEAR',retrieval_state:'READY',evidence_ref:{relation:'public.aos_catalogo_servicios',pk:id,version:'v1'}};}
function ctx(id,name,price){return {entity_id:id,entity_type:'SERVICIO',entity_name:name,category:'TOXINA',quote_price:price,precio_base:price,precio_oferta:price,moneda:'PEN',price_state:'READY',freshness_state:'FRESH',ready_for_quote:true,price_evidence_ref:'catalog:'+id};}
function bundle(){return {version:'R6-TEST',audience:'PUBLIC_CLIENT',authority:'GOVERNED_SOURCE_ONLY',generic_llm_authority:false,items:[item(H1,'HUTOX 1 ZONA 15U'),item(N1,'NABOTA 1 ZONA 15U'),item(H3,'HUTOX 3 ZONAS 50U'),item(N3,'NABOTA 3 ZONAS 50U')]};}
function contexts(){return [ctx(H1,'HUTOX 1 ZONA 15U',299),ctx(N1,'NABOTA 1 ZONA 15U',399),ctx(H3,'HUTOX 3 ZONAS 50U',799),ctx(N3,'NABOTA 3 ZONAS 50U',999)];}

test('R6 restores owner-approved Sofia greeting for toxin first contact',()=>{
  const runtime={state:{treatment:'TOXINA_BOTULINICA'},intents:['INFO']};
  const d=copilot.deterministicOwnerApprovedIntroDraft(runtime,'Hola, quisiera información sobre toxina botulínica',[]);
  assert.ok(d);assert.ok(d.reply.startsWith('¡Hola! 👋 Soy Sofía de Zi Vital.'));
  assert.equal(d.reply.includes('Hablas con el asistente'),false);
  assert.equal(d.reply.includes('proteína purificada'),false);
  assert.equal((d.reply.match(/\\?/g)||[]).length,1);
});

test('R6 greeting-only first contact uses prior approved organic identification flow',()=>{
  const d=copilot.deterministicOwnerApprovedIntroDraft({state:{},intents:[]},'hola',[]);
  assert.ok(d);assert.match(d.reply,/Soy Sofía de Zi Vital/);assert.match(d.reply,/ya eres paciente/i);assert.match(d.reply,/primera vez/i);
});

test('R6 patient renderer canonicalizes Zi Vital and strips replacement-character mojibake',()=>{
  const out=copilot.renderWhatsAppText('En Zi��Vital te ayudamos con **S/ 399**.');
  assert.equal(out,'En Zi Vital te ayudamos con *S/ 399*.');
  assert.equal(out.includes('�'),false);
});

test('R6 short follow-up price stays in deterministic toxin fast lane using carried treatment state',()=>{
  const runtime={state:{treatment:'TOXINA_BOTULINICA'},intents:['TREATMENT_PRICE']};
  assert.equal(copilot.isPriceFastLane(runtime),true);
  assert.equal(copilot.isPriceFastLane({state:{treatment:'TOXINA_BOTULINICA'},intents:['TREATMENT_PRICE','PROMOTION_REQUEST']}),false);
});

test('R6 deterministic toxin price reply contains all governed READY prices and valid citations',()=>{
  const priced=copilot.gatePublicCatalogMoney(bundle(),contexts(),'PRICE_QUOTE',{intents:['TREATMENT_PRICE']});
  const draft=copilot.deterministicToxinPriceDraft(priced,contexts());
  assert.ok(draft);assert.equal(draft.needs_human,false);assert.equal(draft.next_action,'REPLY');
  for(const x of ['S/ 299','S/ 399','S/ 799','S/ 999'])assert.ok(draft.reply.includes(x),x);
  assert.equal((draft.reply.match(/\\?/g)||[]).length,1);
  assert.equal(draft.cited_knowledge_ids.length,4);
  const grounded=knowledge.validateGroundedSuggestion(draft,priced);assert.equal(grounded.ok,true,grounded.error||'grounding');
});

test('R6 owner-copy and price fast lanes execute before expensive adapters and governed full-path fanout',()=>{
  const src=fs.readFileSync(require.resolve('../../app/wa4-copilot'),'utf8');
  const intro=src.indexOf('const introDraft=!clinicalRisk?deterministicOwnerApprovedIntroDraft');
  const price=src.indexOf('if(!clinicalRisk&&isPriceFastLane(runtime))');
  const adapters=src.indexOf('const [campaignCtx,identityCtx]=await Promise.all');
  const governed=src.indexOf('governed=await buildGovernedContext');
  assert.ok(intro>0&&price>intro&&adapters>price&&governed>adapters);
});
""")
          ai_test=Path('ci/wa4-ai-sales-router/ai-router.test.js')
          a=ai_test.read_text(); req="require('./r6-conversation-ux-fastlane.test.js');\n"
          if req not in a: ai_test.write_text(a.rstrip()+"\n"+req)

          # -------- L10 behavioral + static typing tests --------
          t=Path('app/wa-l10-autonomous-bridge.test.js'); ts=t.read_text()
          add="""

test('eligible inbound requests real typing before governed suggestion and canonical send',async()=>{
  const order=[];
  const bridge=createAutonomousBridge({
    serviceRpc:async(name,p)=>{if(name==='aos_wa_l10_bridge_claim_v1')return {data:claim(p.p_provider_message_id)};return {data:{ok:true}};},
    sendTyping:(id)=>{order.push('typing:'+id);return Promise.resolve({ok:true});},
    suggestInternal:async()=>{order.push('suggest');return safeSuggestion();},
    autoSend:async()=>{order.push('send');return {status:200,body:{ok:true,decision_id:'33333333-3333-4333-8333-333333333333',message_id:'wamid.out.typing'}};},
    requestHandoff:async()=>{}
  });
  const r=await bridge.processProviderMessage('wamid.typing');
  assert.equal(r.outcome,'SENT');assert.deepEqual(order,['typing:wamid.typing','suggest','send']);
});

test('typing indicator failure is best-effort and never blocks governed reply',async()=>{
  let sends=0,handoffs=0;
  const bridge=createAutonomousBridge({
    serviceRpc:async(name,p)=>{if(name==='aos_wa_l10_bridge_claim_v1')return {data:claim(p.p_provider_message_id)};return {data:{ok:true}};},
    sendTyping:()=>{throw new Error('META_TYPING_UNAVAILABLE');},
    suggestInternal:async()=>safeSuggestion(),
    autoSend:async()=>{sends++;return {status:200,body:{ok:true,decision_id:'33333333-3333-4333-8333-333333333333',message_id:'wamid.out.typing2'}};},
    requestHandoff:async()=>{handoffs++;},
    log:{error:()=>{}}
  });
  const r=await bridge.processProviderMessage('wamid.typing.fail');
  assert.equal(r.outcome,'SENT');assert.equal(sends,1);assert.equal(handoffs,0);
});
"""
          if "eligible inbound requests real typing before governed suggestion" not in ts:
              t.write_text(ts.rstrip()+add+"\n")

          c=Path('ci/wa-l10/autonomous_bridge_contract.test.js'); cs=c.read_text()
          marker="console.log('WA_L10_AUTONOMOUS_BRIDGE_STATIC_CONTRACT=PASS');"
          typing_contract="""assert(server.includes("typing_indicator:{type:'text'}"),'Meta typing indicator payload missing');
assert(server.includes("status:'read'"),'typing indicator must mark the triggering inbound as read');
assert(server.includes('sendTyping:sendTypingIndicator'),'L10 bridge must receive server-owned typing dependency');
const typingAt=bridge.indexOf('sendTyping(claim.provider_message_id)');
assert(typingAt>claimAt&&typingAt<suggestAt,'typing indicator must start after exact claim and before model work');
assert(!/graph\\.facebook\\.com/i.test(bridge),'bridge itself may not become provider transport');

"""+marker
          cs=replace_once(cs,marker,typing_contract,'typing-static-contract')
          c.write_text(cs)
          PY
      - name: R6 syntax and focused regression gates
        run: |
          node --check app/wa4-copilot.js
          node --check app/wa-l10-autonomous-bridge.js
          node --check app/server-wa4.js
          node --test ci/wa4-ai-sales-router/r6-conversation-ux-fastlane.test.js
          node --test ci/wa4-ai-sales-router/ai-router.test.js
          node --test app/wa-l10-autonomous-bridge.test.js
          node ci/wa-l10/autonomous_bridge_contract.test.js
      - name: Commit scoped R6 product patch
        shell: bash
        run: |
          git config user.name "ascenda-r6-bot"
          git config user.email "ascenda-r6-bot@users.noreply.github.com"
          git add app/wa4-copilot.js app/wa-l10-autonomous-bridge.js app/server-wa4.js app/wa-l10-autonomous-bridge.test.js ci/wa-l10/autonomous_bridge_contract.test.js ci/wa4-ai-sales-router/r6-conversation-ux-fastlane.test.js ci/wa4-ai-sales-router/ai-router.test.js
          git diff --cached --check
          git commit -m "WA-L10 R6: restore Sofia UX, typing indicator and deterministic price fast lane"
          git push origin HEAD:wa-l10-r6-conversation-ux-fastlane-20260905
