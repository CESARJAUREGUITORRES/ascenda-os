name: CONV-L3 Shadow Runtime

on:
  pull_request:
    paths:
      - 'app/conversation-agent-runtime.js'
      - 'app/conv-l3-real-model-benchmark.js'
      - 'app/server-f17.js'
      - 'ci/conv-l3/**'
      - '.github/workflows/conv-l3-shadow-runtime.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  contract:
    runs-on: [self-hosted, Windows, X64, ascenda-fast]
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Syntax
        run: node --check app/conversation-agent-runtime.js && node --check app/conv-l3-real-model-benchmark.js && node --check app/server-f17.js && node --check ci/conv-l3/agent_runtime_contract.js && node --check ci/conv-l3/shadow_benchmark.js && node --check ci/conv-l3/real_model_benchmark_contract.js
      - name: Shadow runtime safety contract
        run: node ci/conv-l3/agent_runtime_contract.js
      - name: Deterministic shadow orchestration benchmark
        run: node ci/conv-l3/shadow_benchmark.js
      - name: Real-model benchmark safety contract
        run: node ci/conv-l3/real_model_benchmark_contract.js
