providers = ["...", "python"]

[phases.install]
cmds = ["npm install"]

# CURRENT runtime is wrapped by KronIA K1. Python only materializes deterministic
# security hardening during build; application runtime remains Node.

[start]
cmd = "node server-k1.js"
