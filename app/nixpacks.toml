providers = ["...", "python"]

[phases.install]
cmds = ["npm install"]

# Los HTML se sirven desde public/. Python se usa únicamente para materializar
# el hardening K1 determinista durante build; no hay framework Python en runtime.

[start]
cmd = "node server-k1.js"
