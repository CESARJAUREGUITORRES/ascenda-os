# PR NOTE — ZERO-COST CI V2

This file exists only to make the infrastructure transition auditable from the repository history.

The PR must not be considered fully certified until the repo-level self-hosted runner is registered and the queued checks execute on label `ascenda-zero-cost-v2`.
