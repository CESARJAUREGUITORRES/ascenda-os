# WA-L5 exact-head AGV2 regression trigger

This marker intentionally changes under `ci/agv2/**` so the existing AGV2 Unified Booking workflow re-certifies BOOK/REBOOK on the exact WA-L5 pull-request head. It contains no runtime or production behavior.
